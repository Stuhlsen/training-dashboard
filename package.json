{
  "name": "training-dashboard",
  "private": true,
  "version": "1.0.0",
  "description": "Radsport-Trainingsdashboard — React (app/) auf GitHub Pages, Datensync via GitHub Actions",
  "type": "module",
  "scripts": {
    "test": "node --test --experimental-test-module-mocks \"tests/**/*.test.js\"",
    "sync": "node scripts/generate-data.js",
    "lint": "npx --yes eslint@9 scripts tests",
    "format": "npx --yes prettier@3 --write \"scripts/**/*.js\" \"tests/**/*.js\""
  },
  "devDependencies": {
    "fallow": "^3.17.0"
  }
}
